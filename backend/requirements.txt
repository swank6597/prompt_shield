# requirements.txt
# Python dependencies for the backend.
fastapi
uvicorn
pydantic
presidio-analyzer
presidio-anonymizer
spacy
requests
jsonschema

# --- Smart LLM Router (cloud providers) ---
# Only needed if using cloud routing (strategy=cloud or strategy=auto):
groq                    # Groq API client (free tier: llama-3.1-8b-instant)
boto3                   # Amazon Bedrock (uses AWS credential chain)
google-genai            # Google Gemini (new SDK, replaces deprecated google-generativeai)

# --- Config management ---
python-dotenv           # Load secrets from .env file (never committed)
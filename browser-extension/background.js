// background.js
// Service worker for the extension. Handles extension lifecycle events,
// message routing between content script and popup, and orchestrates
// the POST /analyze call to the backend before a prompt is allowed through.
